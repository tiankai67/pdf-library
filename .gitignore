# 依赖
node_modules/

# 密码哈希（含管理员密码的派生值），切勿提交
/config.json

# 临时文件
*.tmp

# 本地调试产生的 Edge 无头 profile / 截图
edge-*/
*.png

# 系统文件
.DS_Store
Thumbs.db

# 部署改用分支根发布，不在仓库内保留 workflow（避免需 workflow scope 的 token 才能推送）
.github/
bk-check.json
